All the intresting DP question i came across.  
1 Digit_Dp.cpp Find the number of integers in the range [low,high] which contain the digit d exactly cnt times. In sol we use tight to count excatly less than or not.
